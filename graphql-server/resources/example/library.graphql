type Book {
  title: String!
  authors: [Author!]
}

type Author implements Person {
  name: String!
  knows: [Person]
  description: String
}

interface Person {
  name: String!
  knows: [Person]
}

type Reader implements Person {
  name: String!
  knows: [Person]
  favourite_book: Book
}